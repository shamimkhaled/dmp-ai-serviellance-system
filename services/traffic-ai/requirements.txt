# ── Web / API ─────────────────────────────────────
fastapi>=0.110
uvicorn[standard]
aiohttp>=3.9

# ── Data stores ───────────────────────────────────
redis>=5.0
asyncpg>=0.29

# ── Vision / ML (CPU-only dev; swap torch line for GPU prod) ─────────────────
opencv-python-headless>=4.9
numpy>=1.26
# CPU-only PyTorch — install BEFORE ultralytics to prevent it pulling CUDA build
# For A100 production, replace with: torch torchvision --index-url https://download.pytorch.org/whl/cu122
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
torchvision==0.19.1+cpu
ultralytics>=8.2            # YOLOv8 — loads .pt / .onnx / .engine
deep-sort-realtime>=1.3.2   # DeepSORT multi-object tracker
shapely>=2.0                # Geometric zone checks (polygon/line intersection)

# ── ANPR ──────────────────────────────────────────
paddleocr>=2.7.3            # OCR for Bangladesh plate reading
paddlepaddle>=2.6.1         # CPU backend; swap for paddlepaddle-gpu on A100

# ── Observability ─────────────────────────────────
structlog>=24.1
prometheus-client>=0.20
python-dotenv>=1.0

# ── HTTP client ───────────────────────────────────
httpx>=0.27

# ── Pydantic ──────────────────────────────────────
pydantic>=2
